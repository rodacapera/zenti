name: 🍏 iOS CI/CD - TestFlight (Manual Dispatch & Paths)

on:
  workflow_dispatch:
  push:
    branches:
      - develop
    paths:
      - 'ios/**'
      - 'Podfile'
      - 'Podfile.lock'
      - '.github/workflows/ios-testflight.yml'

jobs:
  build-and-upload-ios:
    runs-on: macos-latest
    timeout-minutes: 45

    steps:
      # 0️⃣ Checkout
      - name: 📥 Checkout
        uses: actions/checkout@v3

      # 1️⃣ Instala Node y deps JS
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.18.1'

      - name: 📦 Install JS deps
        run: yarn install --frozen-lockfile

      # 2️⃣ CocoaPods
      - name: 🧹 Install Pods
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.x'
      - run: |
          cd ios
          pod install --repo-update
          cd ..

      # 3️⃣ Version bump
      - name: 🏷️ Bump version & build number
        run: |
          cd ios
          xcrun agvtool new-marketing-version 1.0.1
          xcrun agvtool new-version -all ${{ github.run_number }}
          cd ..

      # 4️⃣ Build & Export usando firma automática
      - name: 🏗️ Build & Export IPA
        uses: yukiarrr/ios-build-action@v1.12.0
        with:
          workspace-path: ios/Zenti.xcworkspace
          scheme: Zenti
          export-method: app-store
          output-path: ios/build
          team-id: ${{ secrets.APPLE_DEVELOPMENT_TEAM_ID }}
          signing-style: Automatic
          custom-keychain-name: build

      # 5️⃣ Mostrar logs si algo falla
      - name: 🔍 Dump Gym log on failure
        if: failure()
        run: |
          echo "----- Gym Log -----"
          cat ~/Library/Logs/gym/Zenti-Zenti.log || echo "(no log found)"

      # 6️⃣ Verificar IPA
      - name: 📁 Check generated IPA
        run: |
          ls -lh ios/build
          test -f ios/build/*.ipa || (echo "❌ IPA not found" && exit 1)

      # 7️⃣ Subir a TestFlight
      - name: 🚀 Upload to TestFlight
        env:
          APPLE_ID: ${{ secrets.APP_STORE_CONNECT_USERNAME }}
          APP_SPECIFIC_PASSWORD: ${{ secrets.FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD }}
        run: |
          xcrun altool \
            --upload-app \
            --type ios \
            --file ios/build/$(ls ios/build | grep .ipa) \
            --username "$APPLE_ID" \
            --password "$APP_SPECIFIC_PASSWORD" \
            --output-format xml

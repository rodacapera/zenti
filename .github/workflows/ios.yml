name: 🍏 iOS CI/CD - Production

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-14
    timeout-minutes: 45
    env:
      APPLE_TEAM_ID: ${{ secrets.APPLE_DEVELOPMENT_TEAM_ID }}
      IOS_CERTIFICATE_BASE64: ${{ secrets.IOS_CERTIFICATE_BASE64 }}
      IOS_PROFILE_BASE64: ${{ secrets.IOS_PROFILE_BASE64 }}
      IOS_CERT_PASSWORD: ${{ secrets.IOS_CERT_PASSWORD }}
      APP_STORE_CONNECT_USERNAME: ${{ secrets.APP_STORE_CONNECT_USERNAME }}
      FASTLANE_PASSWORD: ${{ secrets.APP_STORE_CONNECT_PASSWORD }}

    steps:
      # 1. Clonar el repositorio
      - name: 📥 Checkout del código
        uses: actions/checkout@v3

      # 2. Configurar Node.js
      - name: 🔧 Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20.18.1'

      # 3. Instalar dependencias con Yarn
      - name: 📦 Instalar dependencias
        run: yarn install --frozen-lockfile

      # 4. Limpiar e instalar pods (deintegrate + install)
      - name: 🧹 Reiniciar pods
        run: |
          cd ios
          pod deintegrate
          pod install --repo-update

      # 5. Importar certificado y perfil de aprovisionamiento
      - name: 🔐 Configurar firma de código
        run: |
          mkdir -p ~/certs
          echo "$IOS_CERTIFICATE_BASE64" | base64 --decode > ~/certs/cert.p12
          echo "$IOS_PROFILE_BASE64" | base64 --decode > ~/certs/profile.mobileprovision

          security create-keychain -p "github" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "github" build.keychain
          security import ~/certs/cert.p12 -k build.keychain -P "$IOS_CERT_PASSWORD" -T /usr/bin/codesign

          mkdir -p ~/Library/MobileDevice/Provisioning Profiles
          cp ~/certs/profile.mobileprovision ~/Library/MobileDevice/Provisioning Profiles/

      # 6. Archivar con xcodebuild (automatic signing)
      - name: 🏗️ Archive iOS App
        run: |
          cd ios
          xcodebuild clean archive \
            -workspace Zenti.xcworkspace \
            -scheme Zenti \
            -configuration Release \
            -sdk iphoneos \
            -archivePath build/Zenti.xcarchive \
            -allowProvisioningUpdates \
            DEVELOPMENT_TEAM=$APPLE_TEAM_ID \
            CODE_SIGN_STYLE=Automatic

      # 7. Exportar .ipa usando tu exportOptions.plist
      - name: 📦 Exportar .ipa
        run: |
          cd ios
          xcodebuild -exportArchive \
            -archivePath build/Zenti.xcarchive \
            -exportOptionsPlist exportOptions.plist \
            -exportPath build \
            -allowProvisioningUpdates

      # 8. Subir a TestFlight con Fastlane
      - name: 🚀 Subir a TestFlight con Fastlane
        run: |
          gem install fastlane
          fastlane pilot upload \
            --username "$APP_STORE_CONNECT_USERNAME" \
            --app_identifier "com.teranov.zentiapp" \
            --ipa "ios/build/Zenti.ipa" \
            --skip_waiting_for_build_processing true \
            --skip_submission true
